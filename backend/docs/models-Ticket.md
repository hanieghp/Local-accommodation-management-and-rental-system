# 📄 models/Ticket.js - مدل تیکت پشتیبانی

## 📋 توضیحات کلی
این فایل Schema و Model تیکت‌های پشتیبانی را تعریف می‌کند. سیستم تیکت امکان ارتباط دوطرفه بین کاربران و ادمین را فراهم می‌کند.

---

## 📝 تحلیل خط به خط

### خط 1: import
```javascript
const mongoose = require('mongoose');
```
- وارد کردن Mongoose

---

### خطوط 3-56: تعریف Schema

```javascript
const ticketSchema = new mongoose.Schema({
    // فیلدها...
});
```

---

## 📊 ساختار کامل Schema

```
┌──────────────────────────────────────────────────────────────┐
│                          Ticket                              │
├──────────────────────────────────────────────────────────────┤
│ _id        : ObjectId (auto)                                 │
│ user       : ObjectId → User (required)                      │
│ subject    : String (max 200, required)                      │
│ category   : Enum [bug|feature|complaint|question|other]     │
│ priority   : Enum [low|medium|high|urgent]                   │
│ status     : Enum [open|in-progress|resolved|closed]         │
├──────────────────────────────────────────────────────────────┤
│ messages   : [{                                              │
│     sender     : ObjectId → User                             │
│     senderRole : Enum [user|admin]                           │
│     message    : String                                      │
│     createdAt  : Date                                        │
│ }]                                                           │
├──────────────────────────────────────────────────────────────┤
│ createdAt  : Date (auto)                                     │
│ updatedAt  : Date (auto, via middleware)                     │
└──────────────────────────────────────────────────────────────┘
```

---

## 📝 تحلیل فیلدها

### فیلد: user (کاربر ایجادکننده)

```javascript
user: {
    type: mongoose.Schema.Types.ObjectId,
    ref: 'User',
    required: true
}
```

- رفرنس به کاربری که تیکت را ایجاد کرده
- اجباری است

---

### فیلد: subject (موضوع)

```javascript
subject: {
    type: String,
    required: [true, 'Subject is required'],
    trim: true,
    maxlength: [200, 'Subject cannot exceed 200 characters']
}
```

| ویژگی | مقدار | توضیح |
|-------|-------|-------|
| required | true | اجباری با پیام سفارشی |
| trim | true | حذف فاصله‌های اضافی |
| maxlength | 200 | حداکثر طول با پیام خطا |

---

### فیلد: category (دسته‌بندی)

```javascript
category: {
    type: String,
    enum: ['bug', 'feature', 'complaint', 'question', 'other'],
    default: 'other'
}
```

| مقدار | معنی فارسی | توضیح |
|-------|------------|-------|
| `bug` | باگ/خطا | گزارش مشکل فنی |
| `feature` | درخواست قابلیت | پیشنهاد ویژگی جدید |
| `complaint` | شکایت | نارضایتی از خدمات |
| `question` | سؤال | سؤال عمومی |
| `other` | سایر | موضوعات دیگر |

---

### فیلد: priority (اولویت)

```javascript
priority: {
    type: String,
    enum: ['low', 'medium', 'high', 'urgent'],
    default: 'medium'
}
```

| مقدار | معنی | رنگ UI | SLA (پاسخ) |
|-------|------|--------|------------|
| `low` | کم | سبز | 72 ساعت |
| `medium` | متوسط | زرد | 48 ساعت |
| `high` | بالا | نارنجی | 24 ساعت |
| `urgent` | فوری | قرمز | 4 ساعت |

---

### فیلد: status (وضعیت)

```javascript
status: {
    type: String,
    enum: ['open', 'in-progress', 'resolved', 'closed'],
    default: 'open'
}
```

**چرخه وضعیت:**

```
     ┌──────────┐
     │   open   │ ← ایجاد تیکت جدید
     └────┬─────┘
          │ ادمین پاسخ می‌دهد
          ▼
   ┌─────────────┐
   │ in-progress │
   └──────┬──────┘
          │ مشکل حل شد
          ▼
   ┌─────────────┐
   │  resolved   │
   └──────┬──────┘
          │ کاربر/ادمین می‌بندد
          ▼
   ┌─────────────┐
   │   closed    │
   └─────────────┘
```

| وضعیت | توضیح | Badge Color |
|-------|-------|-------------|
| `open` | تازه ایجاد شده | 🔴 قرمز |
| `in-progress` | در حال بررسی | 🟡 زرد |
| `resolved` | حل شده | 🟢 سبز |
| `closed` | بسته شده | ⚫ خاکستری |

---

### آرایه: messages (پیام‌ها)

```javascript
messages: [{
    sender: {
        type: mongoose.Schema.Types.ObjectId,
        ref: 'User',
        required: true
    },
    senderRole: {
        type: String,
        enum: ['user', 'admin'],
        required: true
    },
    message: {
        type: String,
        required: true
    },
    createdAt: {
        type: Date,
        default: Date.now
    }
}]
```

**ساختار هر پیام:**

| فیلد | نوع | اجباری | توضیح |
|------|-----|--------|-------|
| sender | ObjectId | ✅ | فرستنده پیام |
| senderRole | String | ✅ | نقش (user/admin) |
| message | String | ✅ | متن پیام |
| createdAt | Date | ❌ | زمان ارسال |

**چرا senderRole؟**
برای نمایش متفاوت پیام‌های کاربر و ادمین در UI:
- پیام کاربر → سمت راست، آبی
- پیام ادمین → سمت چپ، سبز

---

### فیلدهای زمان

```javascript
createdAt: {
    type: Date,
    default: Date.now
},
updatedAt: {
    type: Date,
    default: Date.now
}
```

---

## 🪝 Middleware: بروزرسانی updatedAt

```javascript
ticketSchema.pre('save', function(next) {
    this.updatedAt = Date.now();
    next();
});
```

**توضیح:**
- قبل از هر `save()` اجرا می‌شود
- فیلد `updatedAt` را به زمان فعلی تنظیم می‌کند
- مفید برای مرتب‌سازی بر اساس آخرین فعالیت

---

## 📁 مثال Document کامل

```json
{
    "_id": "65tick123...",
    "user": "65user456...",
    "subject": "مشکل در رزرو اقامتگاه",
    "category": "bug",
    "priority": "high",
    "status": "in-progress",
    "messages": [
        {
            "_id": "65msg001...",
            "sender": "65user456...",
            "senderRole": "user",
            "message": "سلام. من در رزرو کردن اقامتگاه با خطا مواجه شدم. کد خطا: 500",
            "createdAt": "2026-03-10T10:00:00.000Z"
        },
        {
            "_id": "65msg002...",
            "sender": "65admin789...",
            "senderRole": "admin",
            "message": "سلام. لطفاً شناسه اقامتگاه و مرورگر خود را اعلام کنید.",
            "createdAt": "2026-03-10T12:30:00.000Z"
        },
        {
            "_id": "65msg003...",
            "sender": "65user456...",
            "senderRole": "user",
            "message": "شناسه: 65prop111 - مرورگر: Chrome 120",
            "createdAt": "2026-03-10T14:00:00.000Z"
        }
    ],
    "createdAt": "2026-03-10T10:00:00.000Z",
    "updatedAt": "2026-03-10T14:00:00.000Z"
}
```

---

## 🧪 مثال‌های استفاده

### ایجاد تیکت جدید
```javascript
const ticket = await Ticket.create({
    user: req.user.id,
    subject: 'مشکل در پرداخت',
    category: 'bug',
    priority: 'high',
    messages: [{
        sender: req.user.id,
        senderRole: 'user',
        message: 'پرداخت انجام شد ولی رزرو ثبت نشد.'
    }]
});
```

### افزودن پیام جدید توسط ادمین
```javascript
const ticket = await Ticket.findById(ticketId);

ticket.messages.push({
    sender: adminId,
    senderRole: 'admin',
    message: 'مشکل بررسی و حل شد. وجه شما برگردانده می‌شود.'
});

ticket.status = 'resolved';
await ticket.save();  // updatedAt خودکار بروز می‌شود
```

### دریافت تیکت‌های کاربر
```javascript
const userTickets = await Ticket.find({ user: req.user.id })
    .sort({ updatedAt: -1 })
    .select('subject status priority createdAt updatedAt');
```

### دریافت همه تیکت‌ها برای ادمین
```javascript
const allTickets = await Ticket.find()
    .populate('user', 'firstName lastName email')
    .sort({ priority: -1, createdAt: -1 });  // فوری‌ها اول
```

### تغییر وضعیت تیکت
```javascript
await Ticket.findByIdAndUpdate(ticketId, {
    status: 'closed'
});
```

### شمارش تیکت‌های باز
```javascript
const openCount = await Ticket.countDocuments({
    status: { $in: ['open', 'in-progress'] }
});
```

---

## 🎨 نمایش در UI

```
┌──────────────────────────────────────────────────────────────┐
│  🎫 تیکت #65tick123                                          │
│  موضوع: مشکل در رزرو اقامتگاه                                 │
│  دسته: 🐛 باگ    اولویت: 🔴 بالا    وضعیت: 🟡 در حال بررسی   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────┐             │
│  │ کاربر: سلام. من در رزرو کردن با خطا      │ ← 👤 user     │
│  │ مواجه شدم...                              │              │
│  └─────────────────────────────────────────────┘             │
│                                                              │
│         ┌─────────────────────────────────────────────┐      │
│ admin → │ سلام. لطفاً شناسه اقامتگاه را اعلام کنید.  │      │
│         └─────────────────────────────────────────────┘      │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│  [📝 ارسال پیام جدید...]                    [✅ بستن تیکت] │
└──────────────────────────────────────────────────────────────┘
```
