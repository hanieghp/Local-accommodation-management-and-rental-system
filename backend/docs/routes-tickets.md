# 📄 routes/tickets.js - مسیرهای تیکت پشتیبانی

## 📋 توضیحات کلی
این فایل endpoint‌های سیستم تیکت پشتیبانی را تعریف می‌کند. ارتباط دوطرفه بین کاربران و ادمین.

---

## 📊 خلاصه Endpoint‌ها

| متد | مسیر | دسترسی | توضیح |
|-----|------|--------|-------|
| GET | `/api/tickets` | Private | لیست تیکت‌ها |
| GET | `/api/tickets/:id` | Private | جزئیات تیکت |
| POST | `/api/tickets` | Private | ایجاد تیکت جدید |
| POST | `/api/tickets/:id/reply` | Private | پاسخ به تیکت |
| PUT | `/api/tickets/:id/status` | Admin | تغییر وضعیت |

---

## 📝 تحلیل خط به خط

### خطوط 1-6: Import‌ها

```javascript
const express = require('express');
const router = express.Router();
const { body, validationResult } = require('express-validator');
const Ticket = require('../models/Ticket');
const Notification = require('../models/Notification');
const { protect, authorize } = require('../middleware/auth');
```

---

## 📋 Endpoint 1: لیست تیکت‌ها

### مسیر: `GET /api/tickets`

```javascript
router.get('/', protect, async (req, res) => {
    let query = {};
    
    // Admin sees all tickets, users see only their own
    if (req.user.role !== 'admin') {
        query.user = req.user.id;
    }
    
    // Filter by status
    if (req.query.status) {
        query.status = req.query.status;
    }
    
    const tickets = await Ticket.find(query)
        .populate('user', 'name email avatar')
        .populate('messages.sender', 'name avatar')
        .sort({ updatedAt: -1 });
});
```

### دسترسی:
- **کاربر عادی:** فقط تیکت‌های خودش
- **ادمین:** همه تیکت‌ها

### Query Parameters:

| پارامتر | توضیح | مثال |
|---------|-------|------|
| status | فیلتر وضعیت | `?status=open` |

### Response نمونه:
```json
{
    "success": true,
    "data": [
        {
            "_id": "65tick...",
            "user": {
                "_id": "65user...",
                "name": "علی احمدی",
                "email": "ali@example.com"
            },
            "subject": "مشکل در رزرو",
            "category": "bug",
            "priority": "high",
            "status": "open",
            "messages": [
                {
                    "sender": { "name": "علی احمدی" },
                    "senderRole": "user",
                    "message": "سلام، من در رزرو مشکل دارم...",
                    "createdAt": "2026-03-10T10:00:00.000Z"
                }
            ],
            "createdAt": "2026-03-10T10:00:00.000Z",
            "updatedAt": "2026-03-10T10:00:00.000Z"
        }
    ]
}
```

---

## 📖 Endpoint 2: جزئیات تیکت

### مسیر: `GET /api/tickets/:id`

```javascript
router.get('/:id', protect, async (req, res) => {
    const ticket = await Ticket.findById(req.params.id)
        .populate('user', 'name email avatar')
        .populate('messages.sender', 'name avatar');
    
    // Check authorization
    if (req.user.role !== 'admin' && ticket.user._id.toString() !== req.user.id) {
        return res.status(403).json({...});
    }
});
```

**دسترسی:** فقط صاحب تیکت یا ادمین

---

## ➕ Endpoint 3: ایجاد تیکت جدید

### مسیر: `POST /api/tickets`

```javascript
router.post('/', protect, [
    body('subject').trim().notEmpty().withMessage('Subject is required'),
    body('message').trim().notEmpty().withMessage('Message is required'),
    body('category').optional().isIn(['bug', 'feature', 'complaint', 'question', 'other']),
    body('priority').optional().isIn(['low', 'medium', 'high', 'urgent'])
], async (req, res) => {
    const { subject, message, category, priority } = req.body;
    
    const ticket = await Ticket.create({
        user: req.user.id,
        subject,
        category: category || 'other',
        priority: priority || 'medium',
        messages: [{
            sender: req.user.id,
            senderRole: 'user',
            message
        }]
    });
});
```

### Validation Rules:

| فیلد | قاعده | الزامی |
|------|-------|--------|
| subject | notEmpty | ✅ |
| message | notEmpty | ✅ |
| category | isIn([...]) | ❌ |
| priority | isIn([...]) | ❌ |

### مثال Request:
```http
POST /api/tickets
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
Content-Type: application/json

{
    "subject": "مشکل در پرداخت",
    "message": "پرداخت انجام شد ولی رزرو ثبت نشد. شماره تراکنش: 12345",
    "category": "bug",
    "priority": "high"
}
```

---

## 💬 Endpoint 4: پاسخ به تیکت

### مسیر: `POST /api/tickets/:id/reply`

```javascript
router.post('/:id/reply', protect, [
    body('message').trim().notEmpty().withMessage('Message is required')
], async (req, res) => {
    const ticket = await Ticket.findById(req.params.id);
    
    // Check authorization
    const isOwner = ticket.user.toString() === req.user.id;
    const isAdmin = req.user.role === 'admin';
    
    if (!isOwner && !isAdmin) {
        return res.status(403).json({...});
    }
    
    // Add reply
    ticket.messages.push({
        sender: req.user.id,
        senderRole: isAdmin ? 'admin' : 'user',
        message: req.body.message
    });
    
    // If admin replies, set status to in-progress
    if (isAdmin && ticket.status === 'open') {
        ticket.status = 'in-progress';
    }
    
    await ticket.save();
    
    // Create notification for the other party
    if (isAdmin) {
        await Notification.create({
            recipient: ticket.user,
            sender: req.user._id,
            type: 'system_message',
            title: 'New Reply to Your Ticket',
            message: `Admin replied to your ticket: "${ticket.subject}"`
        });
    }
});
```

### جریان پاسخ:

```
┌─────────────────────────────────────────┐
│ کاربر/ادمین پاسخ می‌دهد                 │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│ تعیین senderRole                        │
│ isAdmin ? 'admin' : 'user'              │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│ افزودن به آرایه messages               │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│ اگر ادمین پاسخ داد و status = open:   │
│ status = 'in-progress'                 │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│ اگر ادمین پاسخ داد:                    │
│ ارسال نوتیفیکیشن به کاربر              │
└─────────────────────────────────────────┘
```

---

## 🔄 Endpoint 5: تغییر وضعیت (Admin)

### مسیر: `PUT /api/tickets/:id/status`

```javascript
router.put('/:id/status', protect, authorize('admin'), [
    body('status').isIn(['open', 'in-progress', 'resolved', 'closed']).withMessage('Invalid status')
], async (req, res) => {
    const ticket = await Ticket.findByIdAndUpdate(
        req.params.id,
        { status: req.body.status },
        { new: true }
    );
    
    // Notify user
    await Notification.create({
        recipient: ticket.user._id,
        sender: req.user._id,
        type: 'system_message',
        title: 'Ticket Status Updated',
        message: `Your ticket "${ticket.subject}" status changed to: ${req.body.status}`
    });
});
```

### مثال Request:
```http
PUT /api/tickets/65tick.../status
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
Content-Type: application/json

{
    "status": "resolved"
}
```

---

## 🎨 نمایش در UI

### پنل کاربر:
```
┌──────────────────────────────────────────────────────────────┐
│  🎫 تیکت‌های من                           [+ تیکت جدید]     │
├──────────────────────────────────────────────────────────────┤
│  📌 مشکل در پرداخت                                          │
│  🔴 باز   ⚡ فوری   📅 10 دقیقه پیش                          │
├──────────────────────────────────────────────────────────────┤
│  📌 سوال درباره قوانین لغو                                   │
│  🟡 در حال بررسی   ⬛ متوسط   📅 2 ساعت پیش                  │
└──────────────────────────────────────────────────────────────┘
```

### پنل ادمین:
```
┌──────────────────────────────────────────────────────────────┐
│  🎫 مدیریت تیکت‌ها                                          │
│  [همه] [باز: 5] [در حال بررسی: 3] [حل شده: 12]              │
├──────────────────────────────────────────────────────────────┤
│  👤 علی احمدی                                               │
│  📌 مشکل در پرداخت                                          │
│  🔴 باز   ⚡ فوری   📅 10 دقیقه پیش                          │
│                                           [مشاهده] [پاسخ]   │
├──────────────────────────────────────────────────────────────┤
│  👤 سارا محمدی                                              │
│  📌 درخواست قابلیت جدید                                      │
│  🟢 حل شده   ⬛ کم   📅 دیروز                                │
└──────────────────────────────────────────────────────────────┘
```

### مکالمه تیکت:
```
┌──────────────────────────────────────────────────────────────┐
│  📌 مشکل در پرداخت                        وضعیت: 🟡 در حال  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────┐  👤         │
│  │ سلام. پرداخت انجام شد ولی رزرو ثبت نشد.   │  کاربر      │
│  │ شماره تراکنش: 12345                        │  10:00      │
│  └─────────────────────────────────────────────┘             │
│                                                              │
│  👨‍💼  ┌─────────────────────────────────────────────┐        │
│  ادمین │ سلام. تراکنش بررسی شد. مشکل از درگاه     │        │
│  12:30 │ بود. وجه شما برگردانده می‌شود.            │        │
│        └─────────────────────────────────────────────┘        │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│  [متن پیام جدید...]                              [ارسال]   │
└──────────────────────────────────────────────────────────────┘
```

---

## 📁 Export

```javascript
module.exports = router;
```

این router در `server.js` با prefix `/api/tickets` استفاده می‌شود.
